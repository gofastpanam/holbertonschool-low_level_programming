#include <stdio.h>

/**
 * put with proper grammar, but the outcome is a piece of art,
 * use printf and not the function puts
 * end with the value 0
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
