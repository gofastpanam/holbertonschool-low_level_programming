#include <stdio.h>

/**
* main - prints the alphabet in lowercase
* Prints all possible combinations of single-digit numbers.
* Return: 0 (Success)
*/
int main(void)
{
int c;
for (c = 0; c < 10; ++c)
{
printf("%d,", c);
}
printf("\n");
return (0);
}
