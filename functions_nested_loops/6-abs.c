#include "main.h"

/**
* _abs - Computes the function that computes the absolute value of an integrer.
* @r: The integrer.
* main - Check the code
* Return:
*
*/
int _abs(int r)
{
	if (r < 0)
	{
		r = -r;
	}
	return r;
}
