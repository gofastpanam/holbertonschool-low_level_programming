#include "main.h"

/**
* add - adds two integers and returns the result.
* @a: first integrer
* @b: second integrer
* Return: the result of two integers.
*/
int add(int a, int b)
{
	return (a + b);
}
