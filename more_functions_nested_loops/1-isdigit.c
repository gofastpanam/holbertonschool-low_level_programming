#include "main.h"

/**
* _isdigit - Checks for for a digit (0 through 9).
* @c:the character or digit.
* Return: 1 if c is a digit.
*
*/

int _isdigit(int c)
{
	if (c <= 0 && c >= 0)
	{
	return (1);
	}
	else
	return (0);
}
